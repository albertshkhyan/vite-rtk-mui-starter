export enum PAGE_ROUTES_PUBLIC {
  HOME = "/",
  LOGIN = "/login",
  FORGOT_PASSWORD = "/forgot-password",
}

export enum PAGE_ROUTES_PRIVATE {
  HOME = "/overview",
  OVERVIEW = "/overview",
  SETTINGS = "/settings",
}
